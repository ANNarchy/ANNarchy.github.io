# set_current_step { #ANNarchy.core.Global.set_current_step }

```python
set_current_step(t, net_id=0)
```

Sets the current simulation step (integer).

**Warning:** can be dangerous for some spiking models.